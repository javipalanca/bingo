[project]
name = "bingo"
version = "0.1.0"
description = "Bingo app (Electron + Web/FastAPI)"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.0",
  "reportlab>=4.0.0",
]

[tool.uv]
package = false
